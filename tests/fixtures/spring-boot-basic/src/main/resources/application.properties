# 'ser' is left intentionally incomplete: it is the verified property-validation
# fixture ('ser' is an unknown property. Did you mean 'server.address'?).
ser

# server.port is framework-provided metadata: a hover target that needs only the
# starter classpath. fixture.greeting.salutation is project-provided metadata
# (see GreetingProperties): a hover target and a property-to-definition target
# that resolves to the salutation field.
server.port=8080
fixture.greeting.salutation=hi
