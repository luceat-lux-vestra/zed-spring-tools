<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.5</version>
        <relativePath/>
    </parent>

    <groupId>dev.zed.spring</groupId>
    <artifactId>zed-spring-tools-fixture</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>zed-spring-tools-fixture</name>

    <properties>
        <java.version>21</java.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <!--
            Spring Data JPA puts the repository/@Query annotations and the JPQL
            query intelligence on the classpath. The Spring language server indexes
            statically (it never starts the app), so the embedded-query inlay hints
            (IH-2) need only the annotations to resolve; H2 is a runtime driver so a
            plain `mvn` build still resolves without a real database.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!--
            Generates spring-configuration-metadata.json for the fixture's own
            @ConfigurationProperties at compile time. Without it the Spring
            language server has no metadata to document or navigate for the
            fixture.greeting.* keys, so hover and property-to-definition would
            have nothing project-provided to act on.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>
</project>
