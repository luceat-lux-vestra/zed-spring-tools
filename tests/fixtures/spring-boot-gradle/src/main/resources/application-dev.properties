# A discovered profile. The run/debug generator turns each `application-<profile>`
# file into its own task entry, and on Gradle the profile must be forwarded as a
# bootRun program argument (`--args=--spring.profiles.active=dev`) rather than as
# Maven's `-Dspring-boot.run.profiles=dev` plugin property.
server.port=8081
fixture.greeting.salutation=hi from dev
