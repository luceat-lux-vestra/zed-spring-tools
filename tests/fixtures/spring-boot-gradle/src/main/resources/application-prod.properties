# A second discovered profile, so the gate observes more than one generated entry
# and can tell per-profile generation from a single hardcoded one.
server.port=8082
fixture.greeting.salutation=hi from prod
